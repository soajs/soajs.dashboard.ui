<alert type="danger" ng-if="message.danger">{{message.danger}}</alert>
<br/>
<form name="add">
	<div class="form-group" ng-init="$root.valid = add.$valid" ng-keyup="$root.valid = add.$valid" ng-mousemove="$root.valid = add.$valid">
		<tabset>
			<tab heading="General">
				<br/>
				<div>
					<label>Resource Configuration Name</label>
					<input type="text" class="form-control" ng-model="formData.name" ng-required="true"/>
					<label class="fieldMsg">Choose the name of the new Resource Configuration</label>
					<br/>
				</div>
				<div>
					<label>Resource Type</label>
					<select name="type" id="type" ng-model="formData.type" class="form-control">
						<option ng-repeat="option in formData.types" value="{{option.v}}">{{option.l}}</option>
					</select>
					<label class="fieldMsg">Type of the new resource</label>
					<br/>
				</div>
				<div>
					<label>Resource Category</label>
					<select name="category" id="category" ng-model="formData.category" class="form-control" ng-change="changed('category',formData.category)">
						<option ng-repeat="option in formData.categories[formData.type]" value="{{option.v}}">
							{{option.l}}
						</option>
					</select>
					<label class="fieldMsg">Category of the new resource</label>
					<br/>
				</div>
				<div>
					<label>Resource Configuration</label>
					<div class="customRegistryAceEditor" ui-ace="options.aceEditorConfig" ng-model="formData.config"></div>
				</div>
			</tab>
			<tab heading="Settings">
				<br/>
				<div>
					<label>Plug Resource Configuration</label>
					<label class="toggleSwitch f-right"><input type="checkbox" ng-model="formData.plugged"><span class="buttonSlider round"></span></label>
					<label class="fieldMsg">Turn this on if you want this Resource Configuration to be available for
						your services & daemons</label>
					<br/><br/>
				</div>
				<div>
					<label>Share Resource Configuration With Other Environments</label>
					<label class="toggleSwitch f-right"><input type="checkbox" ng-model="formData.shared"><span class="buttonSlider round"></span></label>
					<label class="fieldMsg">Sharing a Resource Configuration makes it accessible via other
						environments</label>
					<br/><br/>
				</div>
				<div ng-if="formData.shared">
					<div class="grid">
						<table width="100%" border="0" cellpadding="5" cellspacing="3">
							<tbody>
								<tr class="first odd" ng-class="{'first': $first, 'last': $last, 'odd': $odd, 'even': $even}" ng-repeat="env in environments">
									<td width="5%"><input type="checkbox" name="{{env.code}}" ng-model="env.selected"/>
									</td>
									<td width="20%" align="left">{{env.code}}</td>
									<td width="75%">{{env.description}}</td>
								</tr>
							</tbody>
						</table>
					</div>
				</div>
			</tab>
		</tabset>
	</div>
</form>
