<section>
	<div>
		<div>
			<button ng-if="access.kubernetes.storageClass.addItem" class="btn btn-primary" type="button" aria-haspopup="true" aria-expanded="true"
			        ng-click="createItem('StorageClass')">
				Add New StorageClass
			</button>
			<button class="btn btn-primary f-right" type="button" aria-haspopup="true" aria-expanded="true"
			        ng-click="listStorageClass(searchKeyword)">
				Search</span>
			</button>
			<input type="text" class="secret-search form-control f-right" ng-model="searchKeyword"
			       placeholder="Search ..."/>
			
		</div>
		<br>
		<div ng-if="storageClass && storageClass.items.length > 0 ">
			<ul class="blueListing">
				<li class="blueLi row-list active" ng-repeat="item in storageClass.items"
				    ng-style="{'width': '99%'}">
					<div style="width: 20%;display: inline-block">
						<span class="">{{item.metadata.name}}</span>
					</div>
					
					<div style="width: 25%;display: inline-block">
						<span>UID: </span> <span class="">{{item.metadata.uid}}</span>
					</div>
					
					<div style="width: 20%;display: inline-block">
						<span>Created: </span> <span class="">{{item.metadata.creationTimestamp}}</span>
					</div>
					<div style="float: right">
						<a ng-click="inspectItem(item)"
						   ng-style="{'margin-top': '-19px', 'margin-right': '5px'}"><span
								tooltip="inspect storageClass" class="icon icon-info"></span></a>
					</div>
					<div style="float: right">
						<a ng-click="editItem(item, 'StorageClass')"
						   ng-style="{'margin-top': '-19px', 'margin-right': '5px'}"><span
								tooltip="edit storageClass" class="icon icon-pencil2"></span></a>
					</div>
					<div ng-if="access.kubernetes.storageClass.delete" style="float: right">
						<a ng-click="deleteStorageClass(item)" ng-confirm-click="Are you sure you want to delete this storageClass?"
						   ng-style="{'margin-top': '-19px', 'margin-right': '5px'}"><span
								tooltip="delete storageClass" class="icon icon-cross"></span></a>
					</div>
				</li>
				<li class="blueLi row-list" ng-show="storageClass.metadata.continue" ng-style="{'width': '99%'}">
					<a href="" class="loadMore" ng-click="listStorageClass(searchKeyword)">Load More</a>
				</li>
			</ul>
		</div>
		<div ng-if="!storageClass || storageClass.items.length === 0">
			<br/>
			<br/>
			<alert type="warning">No StorageClass found</alert>
		</div>
	</div>

</section>